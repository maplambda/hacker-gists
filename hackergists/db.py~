import redis as redis
import json

redis = redis.StrictRedis(db='test5')

def flush():
    redis.flushdb()

def store(id, gist):
    """ puts an item on the index """
    redis.set(id, json.dumps(gist))

def get(id):
    return json.loads(redis.get(id))

def exists(id):
    return redis.exists(id)
